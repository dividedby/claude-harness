# Stack Decision

Evaluating: [LIBRARY / APPROACH]
Problem it solves: [what]
Current approach: [what we do now]

Research via gh release pages and docs (no external search needed for most):
  gh release list --repo <upstream> --limit 5
  gh repo view <upstream> --json description,updatedAt

Recommendation: use it / skip it / defer - key reasons only.
