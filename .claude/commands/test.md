Run the test suite for this project. Report:
- Total passed/failed count (one line)
- Only the failing tests with error messages
- Skip all passing test output

If all tests pass, just say "All tests pass" and nothing else.
