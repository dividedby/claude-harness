---
description: Run browser UI tests using playwright-cli on the local running app or a given URL.
---

Use the playwright-tester skill to test: $ARGUMENTS

If no URL is given, read docs/project.md to find the local dev URL and test that.
