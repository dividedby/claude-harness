# Personal Overrides (gitignored)
# Add your personal preferences for this specific project here.
# This file is gitignored — only you see it.

# Examples:
# - My local dev runs on port 3001 instead of 3000
# - I prefer verbose logging during debug
# - Skip linting on quick experiments
